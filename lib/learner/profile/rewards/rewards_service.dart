import 'rewards_model.dart';

class RewardsService {
  Future<List<RewardBadgeModel>> fetchUserBadges() async {
    // Replace with API endpoint GET
    await Future.delayed(const Duration(milliseconds: 200));
    return []; // Return list of badges when available
  }
}